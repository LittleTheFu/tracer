#include "scene.h"
#include "mesh.h"
#include "common.h"
#include "vector.h"

int main()
{
    // std::cout << "starting..." << std::endl;
    // Mesh* mesh = new Mesh(Common::BUNNY);
    // std::cout << "ending..." << std::endl;
    Scene scene;
    scene.run();

    return 0;
}