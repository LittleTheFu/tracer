name: C/C++ CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: configure
      run: ./configure
      working-directory: ../
    - name: make
      run: make
      working-directory: ../
    - name: make check
      run: make check
      working-directory: ../
    - name: make distcheck
      run: make distcheck
      working-directory: ../
